#!/usr/bin/python3
"""Greedy algorithm"""
def makeChange(coins, total):
    """Determine the fewest number of coins needed to meet a given amount total."""
    coin_count = 0
    if total <= 0:
        return 0
    if len(coin) <= 0:
        return -1
    return 5
